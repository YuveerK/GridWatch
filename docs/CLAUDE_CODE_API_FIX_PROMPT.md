You are working in the GridWatch repository. Thoroughly remediate the API defects identified in `docs/API_AUDIT.md`, audited against commit `2c58313`. Implement the changes, not just another review or plan.

First read the audit and applicable repository instructions, inspect the current diff, and confirm each finding against current code. The API is Node ESM/Express 5, Prisma 6/PostgreSQL, with X ingestion and Gemini extraction. Preserve the application's purpose: distinguish separate faults, track partial/local restoration, and retain source evidence. Read client consumers when an API contract changes.

There are 25 offline characterization tests under `api/audit/*.characterization.test.js`. They intentionally assert the CURRENT BUGGY behavior. Use them to understand/reproduce the defects, then replace/move them into proper regression tests with desired-behavior assertions. Do not preserve a bug merely to keep these tests green, delete coverage without replacement, or present their passing status as proof of a fix. `api/audit/read-only-data-check.mjs` is a separate read-only diagnostic.

Work in this order:

1. **Secure and coordinate operations (A01, A03, A04, A13).** Protect every admin route and paid manual action with coherent operator authorization that fails closed outside explicitly enabled local development. Make the refresh button work with that model, including allowed-origin/preflight behavior; do not bake an operator secret into a public Vite bundle. Use a database-backed atomic ownership mechanism across manual, scheduler, admin, and CLI entry points. Include owner-checked release/renewal, expiry handling, stale-work recovery, and cleanup on every failure. Preserve chronological linking. Ensure concurrent calls cannot process the same post/fault twice. Do not hold a database transaction open during provider calls.

2. **Prevent ingestion gaps (A02).** Stop deriving a completed checkpoint from the maximum partially stored post ID. Persist a completed high-water mark and recoverable pagination state, or safely replay an incomplete interval. Advance only after the full interval is persisted. Resume after page/write failures, rate limits, caps, restarts, and expired pagination tokens without losing or duplicating posts. Separate fetch and processing budgets and expose backlog/incomplete status.

3. **Make processing, retry, and reprocessing correct (A04–A07, A14, A15, A20).** Atomically commit outage changes, timeline membership, and the per-fault decision. Make graph evidence idempotent per source contribution. Await multi-fault work inside error handling; resume missing faults even when siblings already have decisions. Preserve actionable review/error statuses. Define re-extract versus relink semantics and replace prior contributions safely, recomputing affected old/new outage state. Repeating a reprocess must not increase outages/links/evidence. Handle late/backfilled posts without overwriting newer facts or matching impossible future events. Keep same-name equipment of different types distinct and resolve parents without self-links. Persist the active extraction and its full summary set coherently, remove obsolete summaries, and make backup/restore round-trip that state. Fix tie-break candidate identities to include originating fault identity and fingerprint semantic inputs; invalidate ambiguous old cache entries.

4. **Correct lifecycle and resident-visible state (A08–A11, A19).** Keep status, restoration percentage, restoration timestamp, and locality flags consistent. Preserve the original restoration time unless evidence corrects it. Base planned-work expiration on scheduled windows, including future announcements older than ten days, image/fault schedules, rescheduling, year boundaries, and SAST dates. Do not report extraction FAILED outcomes as zero failures. Do not reset spend/cooldown controls after paid work merely because a later stage fails. Select the latest update per outage before limiting results. Compute map/locality state from that locality's restoration status and all overlapping outages.

5. **Validate configuration and API contracts (A12, A16).** Validate enums, query shapes, positive integer pagination/budgets, dates, ports, scheduler intervals, and ordered score thresholds. Give malformed/oversized bodies proper 400/413 responses. Bound changes/history results and make ordering deterministic. Define zero-limit behavior explicitly; REFRESH_MAX_POSTS=0 must not accidentally mean unlimited processing. Keep public error messages safe and preserve useful internal diagnostics.

6. **Repair bootstrap and address dependencies (A17, A18).** Restore or replace missing seed:geography/relink scripts and align documentation. Fix CSV import on an empty database and validate inputs before mutation. Add a tracked example environment containing no secrets. Investigate the installed prisma/@prisma/config/deepmerge-ts advisory chain, verify reachability, and choose a compatible remediation. Keep Prisma client/CLI versions compatible and rerun generation/schema/tests as appropriate. Do not blindly apply npm audit fix --force, downgrade Prisma to the scanner's suggestion, or force an incompatible transitive override. If no safe patch is available, explicitly document the unresolved advisory and assessed exposure.

7. **Address remaining hardening items from the audit according to demonstrated impact.** Prioritize media streaming limits/trusted URLs, bounded provider/cycle cancellation, graceful shutdown, sweep races, and unbounded public queries. Measure before adding broad caching/index changes. Review the existing golden merge/split failures, add missing negative/multi-fault coverage, and make evaluation capable of failing an explicitly chosen quality gate. Do not overfit rules to a single labelled file.

Implementation constraints:

- Preserve unrelated user changes. Prefer focused fixes over a broad rewrite or unnecessary infrastructure.
- Add forward-only migrations for required data-model changes; do not edit already-applied historical migrations. Design a deterministic, restart-safe compatibility/backfill path for existing data, including checkpoint and summary/fault identities.
- Use mocked X/Gemini/geocoding for normal tests and disposable PostgreSQL for integration tests. Do not run paid live ingestion/extraction, destructive reset/model-test commands, or modify the user's existing database to test changes. Prepare repair/migration commands and dry-run output for review before destructive existing-data work.
- Include meaningful two-worker, ownership-expiry, transaction-failure, crash/restart, retry, and reprocessing tests; mocks alone are insufficient to prove database locking/atomicity.
- Keep the client/API contract aligned with the smallest necessary client changes. Avoid falsely presenting inferred or restored localities as confirmed current outages.
- Do not leave the API or client running in a background terminal when finished. Close temporary listeners, workers, timers, and database connections you start for validation.

Validation and deliverables:

- Run the existing unit suite, converted regression tests, relevant HTTP/Prisma integration tests, schema validation, and a clean migration-chain test on a disposable database.
- Re-run the six golden evaluations against a safe replay/test database when linking behavior changes. The audit baseline F1 values are: links 90.3%, 0910 96.1%, 0911 91.2%, 0912 100%, 0914 96.4%, 0915 100%. Report precision/recall and explain regressions or label corrections; the old evaluator's exit code alone is not a pass criterion. Do not change labels just to improve scores.
- Run dependency audit and relevant build checks. Clearly distinguish executed tests, blocked/unavailable checks, and unverified claims.
- Maintain a compact A01–A20 resolution table with status, changed files, verification, and any unresolved limitations. Include the additional hardening items separately.
- Finish with the concrete behavior changes, tests/results, remaining issues, and any migration or operator setup steps. Complete all feasible authorized code/test/documentation work before requesting any final approval needed for destructive data operations.
